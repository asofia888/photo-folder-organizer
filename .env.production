# Production environment variables for Vite.
# Only VITE_-prefixed variables are exposed to the client bundle.
# Do NOT set NODE_ENV here — Vite manages the mode itself (setting it
# triggers a build warning).
